from setuptools import setup
from setuptools import find_packages

setup(name='huggingface_utils',
      version='0.0.1',
      description='useful function for huggingface',
      author='The fastest man alive.',
      packages=find_packages(),
      install_requires=[])